exclude-paths:
  - ".git"
  - ".devcontainer"
  - "templates"
  - "simple_test"

output-path: "reports" 
output-name: "kics-report"
report-formats:
  - json
  - sarif
fail-on:
  - critical
  - high
  - medium
  - low
ignore-on-exit: none

