exclude-paths:
  - ".git"
  - ".devcontainer"
  - "templates"

output-path: "reports" 
output-name: "kics-report"
report-formats:
  - json
  - sarif
fail-on:
  - critical
  - high
  - medium
  - low
ignore-on-exit: none

